server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/loginsignup_db
    username: root
    password: Onkar@11
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:anuj}
    password: ${MAIL_PASSWORD:123}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  security:
    user:
      name: ${SECURITY_USERNAME:anuj}
      password: ${SECURITY_PASSWORD:1234}

openai:
  api-key: https://chatgpt.com/c/69064805-5f5c-8320-aa52-20411388799e